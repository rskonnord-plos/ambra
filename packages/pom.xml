<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>org.topazproject.packages</groupId>
  <artifactId>packages</artifactId>
  <packaging>pom</packaging>
  <version>0.7</version>
  <name>packages</name>

  <parent>
    <groupId>org.topazproject</groupId>
    <artifactId>the-whole-shebang</artifactId>
    <version>0.7</version>
  </parent>

  <pluginRepositories>
    <pluginRepository>
      <id>Maven Snapshots</id>
      <url>http://cvs.apache.org/maven-snapshot-repository/</url>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
      <releases>
        <enabled>false</enabled>
      </releases>
    </pluginRepository>

    <pluginRepository>
      <id>Codehaus Snapshots</id>
      <url>http://snapshots.maven.codehaus.org/maven2/</url>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
      <releases>
        <enabled>false</enabled>
      </releases>
    </pluginRepository>
  </pluginRepositories>

  <dependencies>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>1.2.14</version>
    </dependency>
  </dependencies>

  <properties>
    <!-- 
    Name of the Tomcat file. Note: We are assuming that the name of the file
    will correspond to the directory name where the ant task will unzip the
    file to under {project.build.directory}
    -->
    <tomcatFile>apache-tomcat-5.5.20</tomcatFile>
    <tomcatLocation>http://www.apache.org/dist/tomcat/tomcat-5/v5.5.20/bin</tomcatLocation>
  </properties>

  <modules>
    <module>mulgara</module>
    <module>plosone</module>
    <module>cas</module>
    <module>fedora</module>
    <module>tools</module>
  </modules>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>2.2-t1</version>
          <executions>
            <execution>
              <phase>package</phase>
              <goals>
                <goal>attached</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <tarLongFileMode>gnu</tarLongFileMode>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.topazproject.plugins</groupId>
        <artifactId>ant-tasks</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <goals>
              <goal>get-tomcat</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <tomcatFile>${tomcatFile}</tomcatFile>
          <tomcatLocation>${tomcatLocation}</tomcatLocation>
        </configuration>
        <inherited>false</inherited>
      </plugin>

      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <descriptors>
            <descriptor>src/main/assembly/common.xml</descriptor>
          </descriptors>
        </configuration>
        <inherited>false</inherited>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>rpm</id>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.codehaus.mojo</groupId>
              <artifactId>rpm-maven-plugin</artifactId>
              <version>2.0-alpha-3-t1</version>
            </plugin>
            <!-- Put rpm into local repository and ~/.m2/rpms directory -->
            <plugin>
              <artifactId>maven-antrun-plugin</artifactId>
              <executions>
                <execution>
                  <id>copy-rpm</id>
                  <phase>install</phase>
                  <configuration>
                    <tasks>
                      <copy flatten="true" todir="${settings.localRepository}/org/topazproject/packages/${project.name}/${version}">
                        <fileset dir="${project.build.directory}/rpm/RPMS/noarch" />
                      </copy>
                    </tasks>
                  </configuration>
                  <goals><goal>run</goal></goals>
                </execution>
              </executions>
            </plugin>

          </plugins>
        </pluginManagement>

      </build>
    </profile>
  </profiles>

</project>

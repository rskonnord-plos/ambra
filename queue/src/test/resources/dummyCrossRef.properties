#Dummy value for the unit test
ambra.services.crossref.guestquery.url=http://localhost.dev.null/crossref

#For the unit test, we actually want the mailer to send as
#We override the implementation and check an in memory mail box
ambra.queue.sendmail.sendMode = production
